# 实验史记 · 马刀风云

据音克思《实验史记》（2024—2026）改编的回合制战棋游戏。玩法蓝本为原书**卷八《马刀书》**
所载课桌游戏"马刀"：猜拳定行动，买刀买马，同城马踢，血祭翻倍，活者为王。

## 运行

- 双击 `实验史记·马刀风云.html`（单文件打包版，无需服务器，file:// 直接可玩）。
- 或以源码方式打开 `index.html`（同样 file:// 可玩，普通 `<script>` 无模块依赖）。

## 目录结构

| 路径 | 内容 |
|------|------|
| `js/data.js` | 数据卷：33 名角色（17 可选）、16 个剧情关卡、22 项成就、9 种生存增益 |
| `js/scenes.js` | 剧本卷：序/终章、十六卷战前战后对话剧、战中触发台词 |
| `js/engine.js` | 战斗引擎：马刀规则、猜拳、AP 经济、状态、AI、多波次、生存模式 |
| `js/ui.js` | 界面与 Canvas 渲染、菜单、图鉴、成就、弹层、特效 |
| `js/audio.js` | WebAudio 程序合成音效与五声音阶拨弦背景乐（零外部资源） |
| `js/save.js` | localStorage 存档（进度/成就/纪录/设置） |
| `css/style.css` | 宣纸 · 墨 · 朱砂视觉主题 |
| `实验史记·马刀风云.html` | 单文件打包版（构建产物） |

## 开发与测试

```bash
python build.py       # 打包单文件（确定性 LF 输出 + 自检）
node stress.mjs       # 无头压力测试：16 关 + 全角色 + 生存（需 Node）
node winrate4.mjs     # 关卡难度模拟（kiting bot 胜率）
node test_balance.mjs # 角色强弱（中立基准，可传角色列表）
node test_bugfix.mjs  # 断言级修复验证
node test_review.mjs  # 体检修复断言（可怡目标 / 最低血记录）
node test_aiaggr.mjs  # AI 四档行为差异
python test_e2e.py    # Playwright 端到端（channel=msedge，file:// 加载）
python test_bundle.py # 对打包单文件跑同一套 E2E
python test_gauntlet.py  # 16 关连续通关 + 剧情演出
python test_boon.py      # 生存增益弹窗抗覆盖（静置 7 秒）
python test_wave_story.py # 剧情多波次依次登场
# 报告：md2docx.cjs 生成 docx，export_pdf.py 经 Word COM 导出 PDF
NODE_PATH=$(npm root -g) node md2docx.cjs 技术报告.md 实验史记·马刀风云-技术报告.docx
python export_pdf.py 实验史记·马刀风云-技术报告.docx 技术报告.pdf
```

打包（index.html + css + js → 单文件）：

```bash
python - <<'EOF'
import io, re
html = io.open('index.html', encoding='utf-8').read()
css = io.open('css/style.css', encoding='utf-8').read()
html = html.replace('<link rel="stylesheet" href="css/style.css">', '<style>\n' + css + '\n</style>')
html = re.sub(r'<script src="([^"]+)"></script>',
              lambda m: '<script>\n' + io.open(m.group(1), encoding='utf-8').read() + '\n</script>', html)
io.open('实验史记·马刀风云.html', 'w', encoding='utf-8').write(html)
EOF
```

## 平衡设计

- **敌我行动点**：出拳胜4/和3/负2；敌方按难度 简单2/普通3/困难4 动（3人以上降为1/2/2动）。
- **以寡敌众**：玩家每多面对一名敌人，行动点+1（≤+3）、血上限+1（≤+2）。
- **敌方伤害缩放**（`enemyDmgScale`）：1人×0.9、2人×0.88、3人×0.75、4人×0.65，难度再乘 0.7/1/1.15。
- **敌方血池缩放**：剧情/生存中 2/3/4 名敌人时血量为 0.85/0.7/0.62（乱斗模式另按难度 0.8/1/1.2）。
- **合力加成**（`dyad`）仅当敌方恰好 2 人时生效——"情比金坚/TGO合力/DDB同德"本是两人之事。
- **遣返回家**（含笑）：标记载原位置，跳过一个回合后归位；归位判定排除自身占位。

kiting bot 实测（n=20，普通/困难）：序章 85/90%，卷一 65/40%，TGO 60/35%，
修逸 75/70%，DDB 25/10%，崇国 40/25%，二中番外 50/25%。

## 修复记录：弹层互相覆盖

**症状**：生存模式清完一波后，增益三选一会被下一回合的猜拳弹窗顶掉（看选项稍慢就撞上）。

**根因**：`_endRound()` 里调用 `_checkBattleEnd()` 未 await，`_nextWave()`（内含 `await pickBoon`）
成为脱钩的 promise；主循环随即进入下一回合，`rpsRound()` 用同一个 `#modal-box` 覆盖了增益弹层，
而增益的 promise 永不 resolve。

**修复**：
1. `_checkBattleEnd()` 改为 async，`_endRound()` 中 `await this._checkBattleEnd()` —— 波次流程（含弹层）
   结束后才进入下一回合；
2. 新增弹层串行队列 `queueModal()`，猜拳 / 增益三选一 / 剧情对话共用 `#modal-box`，排队执行，
   从结构上杜绝任何互相覆盖。

回归：`test_boon.py`（静置 7 秒不被顶掉 + 连过 3 波）、`test_wave_story.py`（卷一三波次依次登场，
waveIndex 达 2、触发台词 2 条）、`stress.mjs` 全关卡、e2e ×2 全绿。

## 体验改进（第三轮，全部落地）

| 项 | 实现 |
|---|---|
| 猜拳自动化 | `settings.rpsMode = ask/auto`；自动档直接判定并压缩动画 |
| 战中情报 | 点敌军弹图鉴；悬停显示血量/行动点/技能全文（不再截断）；敌军列表显示行动点圆点 |
| 移动撤销 | 引擎 `_pushUndo/undoMove/lockUndo`：仅移动可退且退还行动点，出手即封 |
| 结算列成就 | `SAVE.beginAchLog/takeAchLog`，结算页展示"本场新解锁" |
| 减少动效 | `settings.fx = full/lite/off`：精简限粒子、关闭去粒子+震屏+红晕+花瓣 |
| 存档导入导出 | `SAVE.exportJSON/importJSON` + 设置页文本框收发 |
| 断点续战 | 每回合末 `SAVE.saveBattle(serialize())`；`Battle.fromSave()` 还原；标题页"继续上局" |
| 触屏适配 | 点按代替悬停、`touch-action: manipulation` |
| 关卡地形 | 关卡可配 `blocked` 障碍；`passable()` 贯通寻路/移动/击退/闪现/召唤/刷怪；5 关已配课桌讲台等 |
| 稀有增益 | 6 种质变型（血祭免费/溅射/马踢射程/击杀回血/护盾/首击），抽选固定"2普通+1稀有" |
| 二周目与挑战 | 全通后标题页出现"二周目·困难"；新增 3 项成就（二周目/铁人/十七人皆执刀，按角色记录胜场） |
| 附带修复 | 崇国「种树」无上限导致战斗被拖长 → 场上至多 3 株 |

回归：`stress` 全关卡、`test_features.mjs`（地形/撤销/稀有增益/存档往返/种树上限）、
`test_ux.py`（浏览器实测 10 项）、既有 6 套浏览器回归、e2e ×2 —— 全部 ALL PASS。

## 技术报告

`技术报告.md`（源）/ `实验史记·马刀风云-技术报告.docx` / `技术报告.pdf`（16 页）
—— 覆盖架构、核心机制、AI、表现层、叙事、平衡方法论、测试体系、12 项缺陷档案与后续建议。
DOCX 由 `md2docx.cjs`（自写的 Markdown→DOCX 转换器，处理表格/代码块/行内代码/分页控制）生成，
`export_pdf.py` 经 Word COM 导出 PDF，供排版视觉验收。

## 整体体检（第二轮）

### 又找到并修复的真 bug
1. **可怡「一糖之恩」把治疗给了敌方**：AI 选技能目标时只从 `opponentsOf()` 里挑，而这是治疗友军的技能，
   结果她在卷六里给玩家加血。修复：治疗类技能单独取"己方最疼者"为目标。
   （修好后卷六反而变难了——她终于会真的救大展了，见下。）
2. **成就「高考激励（以1血获胜）」实际拿不到**：击破敌人回血 +2，最后一击必然把血量抬到 3。
   修复：改为记录**全程最低血量** `stats.minHp`（在 dealDamage / rawHurt / 血祭 / 回合末 / 开局五个点采样），
   成就判定改为"曾在 1 血存活并获胜"，描述同步。

### 关卡难度体检（随机猜拳，kiting bot）
全 16 关扫描后，两个关卡 0%、一个 19%，属"不可通关"级别，均已修复：

| 关卡 | 修复前 | 修复后 | 措施 |
|------|-------|-------|------|
| s8 世界马刀协会锦标赛 | **0%** | 28–50% | 三连战改为赛事间**充分休整**（回满血）+ 关卡血量系数 0.68 |
| s14 王崇国本纪 | **0%** | 25–69% | 新增友军**为兵**（原著中为崇国所贬，同仇敌忾）+ 血量系数 0.78 |
| s6 展怡列传 | 19% | 63% | 情侣组合（可怡会疗伤+大展加成）血量系数 0.7 |

- 新增机制：关卡可配置 `hpScale`（血量系数）与 `restFull`（阵间回满），供后续微调使用。
- **崇国「评职称」削弱**：原为"全场沉默 + 行动点-1"，等于每三回合锁死玩家一轮，改为只沉默（不能施技）。

### 难度现状（机器人实测，普通难度）
序章 100%、卷一 88%、含笑 94%、绍歆 75%、六班老师 75%、TGO 75%、**展怡 63%**、
修逸 94%、**锦标赛 28–50%**、皇家曼彻斯特 50%、王子琛 81%、**DDB 41%**、
李帆 100%、李默相东 63%、**崇国 69%**、二中番外 94%。

## 本轮平衡与修复（数据驱动，第一轮）

以 `test_balance.mjs`（每人 24 场 vs 中立路人）与 `test_duel.mjs`（对真实角色）量化，
不凭感觉调数值。

**削弱 鲁豪**（`test_duel` 90% → 77%）
- 血量 20 → **14**；「所有攻击翻倍」→ **仅刀击翻倍**（马踢 6→3、技能 4→2，不再与血祭叠成 4 倍）
- 新增符合原著的代价：**每回合行动点-1（下限2）**。下限保证他以敌方身份出现在卷十一时
  仍有 2 动，不会退化成不动。

**加强弱势**（实测值）
- 歆慧 33%→63%：血 11、技能 2 伤、被动改「每第二回合 +1 行动点」
- 大展 44%→71%：血 12、玉米为刃 3 伤
- 呱宇 56%→88%：技能"额外移动"未实现 → 改为真的 +2 行动点
- 绍铭 56%→79%：血 11、纸条 3 伤
- 只因 50%→71%：血 11、坤拳自伤 30%→15%
- 仙女 54%→71%：怒斥滚射程 2→3
- 汶斌 67%→79%：被动「秒之」此前**完全未实现**，补上（刀击 30% 双倍）
- 头哥 12 血、三溴化氮 2 伤；小蛙 拍肚皮 2 伤

**修复的真 bug**
1. 汶斌被动「秒之」：数据里写了，引擎里从未实现（`calcDamage` 无 wenbin 分支）
2. 呱宇技能：`st.freeMoves = 2` 只写不读，"额外移动两格"从未生效；改为 +2 行动点（描述同步）
3. 成就「搬水者气概也」永远无法达成：`totals.heals` 从未累加（判定处读了却没人写）
4. `doKnife` 未校验缴械：被缴械时仍可经非 UI 路径出刀；已加 guard 与提示

关卡难度复测（n=24）：卷十一 54%、卷三 54%、卷六 46%、卷九 46%、卷七 88%。

## 角色调整记录

**刘鲁豪（加强）**：血量 12 → **20**；被动「大腹如斗」改为**生命上限20，且所有攻击数值翻倍**
（刀击 1→2、马踢 3→6、技能亦翻倍，可与血祭叠加为 4 倍）；原技能「扣水卡」改为单技能
「锦绣昼行」（距三内一敌受2伤，且其下回合行动点-2）。
他在卷十一·DDB列传中是敌方，受伤亡人数缩放后为 14 血，该关 kiting bot 胜率稳定在 43–48%
（仍为全书最难一关，但可打通）。

## AI 进攻性（可设置）

设置页四档，存于 `settings.aiAggr`，出征时写入 `cfg.aiAggr` → `battle.aiAggr`：

| 档位 | 用技意愿 | 血祭 | 残血撤退 | 逡巡 | 目标选择 | 买马意愿 |
|------|---------|------|---------|------|---------|---------|
| passive 消极 | 0.35 | 0.04 | 0.70 | 0.55 | 最近 | 0.30 |
| measured 守成 | 0.65 | 0.20 | 0.35 | 0.25 | 最近 | 0.70 |
| active 主动（默认） | 0.90 | 0.40 | 0.10 | 0 | 残血优先 | 1.0 |
| frenzy 狂攻 | 1.00 | 0.75 | 0 | 0 | 残血优先 | 1.0 |

角色自身的 `aggr` 会在此基础上微调（`prof.skill + (aggr-0.6)*0.25`）；友军固定使用"主动"档。
行为实测见 `test_aiaggr.mjs` / `test_retreat.mjs`。

## 剧情系统

- 战前/胜利/败局对话剧（`SJI_SCENES.stages[id]`），台词多取原文；
- 战中触发器：`roundStart`(回合)、`waveStart`(波次)、`playerLow`、`enemyLow(char)`、`enemyDown(char|any)`；
- 调试：`SJI_DEBUG.skipScenes` 跳过全部对话（e2e 用），`drain_dlg` 负责点击 `#dlg-skip`（gauntlet 用）；
- 序章《序》仅首次进入 s0 播放（存档 `prologueSeen`）；s15 胜利后播《终章》。

## 平衡性（kiting bot 模拟，n=24，角色轮换）

序章/含笑/修逸 ≈ 96–100%，前期 45–85%，卷九以后 13–30%。
bot 不会真正地防守反击，人类玩家实际体验应显著更易；后期关卡设计预期为「硬核但有重开价值」。

## 已知取舍

- 剧情为每卷一关的改编而非全文复刻，原文节选见各关「图鉴」与结算页「音克思曰」。
- 猜拳经济由原作的"胜几人得几动"简化为胜4/和3/负2，多敌时敌方每人1动，保证1vN可玩。
- 存档在浏览器 localStorage（键 `shiji_madao_v1`），换浏览器/清缓存会丢档。
